<h1>New transaction</h1>

<% form_for(@transaction) do |f| %>
  <%= f.error_messages %>

	Amount (£): <%= f.text_field :amount, :label => 'Amount'%>
	<br/>
	Description: <%= f.text_field :description, :label => 'Description'%>
  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', transactions_path %>
